import React from 'react';
import {weatherAPI} from "../../API/api";

const MoreWeather = (props) => {

    // weatherAPI.getHourlyWeather(50.9216, 34.8003).then(data => {console.log(data) })

    return (
        <div>
            <h2>City Name</h2>

        </div>
    );
};

export default MoreWeather;
